<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyLearningStrategyMobleXForms.AppMasterDetailDetail"
             Title="Detail"
             BindingContext="UserProfile">
  <StackLayout Padding="10">
        <Label    Text="User"/>
        <Label  Text="{Binding UserProfile.UserProfileId}"/>
        <Label Text="{Binding UserProfile.DisplayName}"/>
        <Label Text="{Binding UserProfile.FirstName}" />
        <Label Text="{Binding UserProfile.LastName}" />
    </StackLayout>
</ContentPage>